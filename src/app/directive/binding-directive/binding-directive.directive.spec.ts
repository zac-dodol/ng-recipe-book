import { BindingDirectiveDirective } from './binding-directive.directive';

describe('BindingDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new BindingDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
